"""Application layer — use cases, design patterns and ports.

Depends only on the :mod:`domain` layer.  Infrastructure adapters implement
the ports declared here (:mod:`attendance_report.application.interfaces`).
"""
